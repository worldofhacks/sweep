[project]
name = "sweep"
version = "0.0.1"
description = "One person commands 4 to 6 indoor drones through webcam gestures or spoken natural language, and sees what the swarm sees on a laptop console."
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
dev = [
  "pytest>=9,<10",
  "ruff>=0.16,<0.17",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
addopts = ["--import-mode=importlib"]
pythonpath = ["."]
testpaths = ["tests", "relay", "planner", "arbiter", "adapters", "perception", "language", "evals"]

[tool.ruff]
line-length = 100
target-version = "py312"
extend-exclude = ["console", "datasets", "*.md"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
