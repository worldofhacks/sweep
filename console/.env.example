# Public browser configuration only. Never put a Clerk secret key in VITE_*.
# Create your Clerk application, enable Apple / Google / X, then rebuild.
VITE_CLERK_PUBLISHABLE_KEY=
# Optional trusted account API origin; defaults to this application's origin.
# This is deployment configuration, never taken from an invitation or operator connection.
VITE_ATLAS_API_ORIGIN=
