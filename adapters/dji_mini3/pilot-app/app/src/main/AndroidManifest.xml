<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DjiPilotApplication"
        android:allowBackup="false"
        android:label="Sweep DJI Pilot"
        android:theme="@style/Theme.SweepPilot">
        <meta-data android:name="com.dji.sdk.API_KEY" android:value="${DJI_APP_KEY}" />
        <activity android:name=".PilotActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
