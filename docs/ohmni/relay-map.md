# Sweep relay map (main @ cf73068) for device classes, a sensor frame, and a node kit

Repo: /Users/quietguy/capystone. Python 3.12 (`requires-python >=3.12,<3.13`), uv, pytest (`--import-mode=importlib`, `pythonpath=["."]`, testpaths tests, relay, planner, arbiter, adapters, adapters/dji_mini3, perception, language, evals), ruff (line-length 100, py312, select E F I B UP). `just test` = `uv run pytest`; `just lint`; `just ci`. CI `.github/workflows/ci.yml` jobs python, console, bridge-jvm, android-unit, m14-browser.

## Frame router
- `RelaySession.process_frame` relay/session.py:389-410 dispatches on principal.source + raw["type"]: adapter + membership/telemetry/acknowledgement -> process_membership :815 / process_telemetry :854 / process_acknowledgement :913; adapter + type in NODE_FRAME_TYPES -> process_node_frame :1142; localization + control_localization -> :1041; else protocol_refusal frame_not_allowed.
- `NODE_FRAME_TYPES = frozenset({"capabilities","capture_bundle","media_file","capture_readiness","node_status"})` relay/contracts.py:107-109. `_NODE_FRAME_PARSERS` session.py:134-140; `_parse_node_frame` :2731; `_node_frame_identity` :2739; `NodeFrame` union :118-124.

## auth (unsigned first frame)
- `relay.auth.authenticate` relay/auth.py:63-97: `{"v":1,"type":"auth","source","token"}` + `drone_id` iff source in {adapter, localization}; drone_id int 1..2**31-1.
- Keys: `StaticCredentialResolver.resolve(source, drone_id)` auth.py:49-60: adapter -> adapter_keys[drone_id] (or relay_token when allow_shared_adapter_token); console/keyboard/webcam/language -> relay_token. `Principal(source, drone_id, signing_key)`.
- Reply `auth.accepted` (app.py:1324 `_auth_accepted`) carries `node: RelaySettings.node_settings()` (settings.py:272-279: command_ttl_ms, virtual_stick_hz, watchdog_hold_ms, watchdog_failsafe_ms) or `auth.refused` (:1339). One socket per (session, drone_id): `RelayRuntime.subscribe` app.py:372-413.

## membership (node -> relay, SIGNED)
- `parse_membership_request` contracts.py:645-733; dataclass `MembershipRequest` :156-197. Required: v, t, type="membership", event_id, session, drone_id, action, signature; `_exact_fields` (:1276) = no extra, no missing.
- join: + `adapter_id` (<=512 B), `capabilities` (non-empty unique str list <=64 items). readiness: + connection_epoch, home_pose_confirmed, control_authority, rc_safety_operator_present (bools). graceful_leave: + connection_epoch.
- Signing: HMAC-SHA256 over `unsigned_event()` (frame minus signature) canonicalized by `relay.auth.canonical_event_bytes` auth.py:119-135 (`json.dumps(ensure_ascii=False, allow_nan=False, separators=(",",":"), sort_keys=True)`), lowercase hex; verify `verify_event_signature` auth.py:106-116 at session.py:834-839.
- Order/replay: `_check_adapter_binding` :2060; `_claim_transport_event` :2009-2051; `_timestamp_error` :2053 (future_timestamp / stale_timestamp); duplicate event_id -> replayed_event; monotonic t -> out_of_order_event.
- Apply: `_apply_membership` :1865 -> `FleetRegistry.apply_join` state.py:202 / `apply_readiness` :261 / `apply_graceful_leave` :311; `_record_transition_and_state` :1893. Event shape `MembershipTransition.to_event` state.py:57-74 (action, drone_id, connection_epoch, membership, roster_version, reason, readiness_reasons, adapter_id, capabilities, provenance).

## telemetry v1 (node -> relay, unsigned)
- `parse_telemetry` contracts.py:736-794; `TelemetryV1` :200-258 (to_event, state_payload). Keys: v, t, type="telemetry", event_id, session, `drone` (not drone_id), connection_epoch, x, y, z, vx, vy, vz, battery, state, link, pos_quality. battery/link/pos_quality clamped [0,1]; `state` free text <=128 B (not enum-validated).
- `FleetRegistry.apply_telemetry` state.py:358-397 (out_of_order_telemetry, readiness recompute, telemetry_stale/recovered). Audit: every accepted telemetry appended in full (session.py:894-896).

## node_status / capabilities / capture_readiness / media (node -> relay, unsigned)
- `parse_node_status` contracts.py:1114-1153; `NodeStatusFrame` :592-642: envelope (v,t,type,event_id,session) + drone_id, connection_epoch, virtual_stick_enabled, control_authority, authority_change_reason, watchdog_state (nominal|hold|failsafe :76-79), video_publish_state (stopped|connecting|publishing|failed :82-86), phone_battery_percent, phone_thermal_state (:89-96). `FleetRegistry.apply_node_status` state.py:482-489 latches video_publishing_at.
- `parse_capabilities` contracts.py:886-953; `CapabilitiesFrame` :344-408: envelope + drone_id, connection_epoch, native_panorama_modes, photo_capture, gimbal_pitch_min_deg, gimbal_pitch_max_deg, horizontal_fov_deg, storage_remaining_bytes, media_retrieval, aircraft_model, aircraft_firmware, rc_firmware, phone_model, android_version, sdk_version, measured_hfov_deg. `apply_capabilities` state.py:476-480.
- `parse_capture_readiness` :1038-1111; retained in session._capture_readiness (`_remember_capture_readiness` session.py:1457; accessor `capture_readiness(drone_id)` :1387).
- media_file/capture_bundle: parsers :956/:972; never fanned out (session.py:1199-1200); retention caps `_check_media_retention_capacity` :1416.

## command (relay -> node, signed with the node's key)
- `command_event` contracts.py:1229-1267; `parse_command` :844-883; `CommandFrame` :296-341. Keys: envelope + command_id, intent_id, roster_version, drone_id, connection_epoch, seq, issued_at, ttl_ms, operation, args, signature.
- `operation` in planner.models.CommandOperation (planner/models.py:114-127): takeoff, goto, rotate_to, hover, land, estop, camera_capabilities, set_gimbal_pitch, camera_ready, capture_panorama, capture_photo, retrieve_media. Args exact per op `COMMAND_ARGUMENT_FIELDS` contracts.py:129-148 (`takeoff {z_mm}`, `goto {x_mm,y_mm,z_mm,speed_mm_s}`, `rotate_to {yaw_mdeg,speed_mdeg_s}`, ...), enforced by `_command_arguments` :1418.
- Issuer `RelaySession.issue_command` session.py:1208-1296 (seq per (drone_id, epoch) from 1; ttl = limits.command_ttl_ms; audits unsigned then signs). Delivery `RelayNodeLink.send` relay/bridge.py:52-84 -> `RelayRuntime.deliver_to_node` app.py:359-370.

## acknowledgement (node -> relay)
- `parse_adapter_acknowledgement` contracts.py:797-841; `AdapterAcknowledgement` :261-293: envelope + intent_id, command_id, status (LifecycleStatus minus refused), drone_id, connection_epoch, roster_version, reason, detail. Node reasons `NodeAcknowledgementReason` :56-63 = stale_command, out_of_order_command, authority_lost, watchdog_hold, watchdog_failsafe. `_check_acknowledgement` session.py:1914; `await_command_acknowledgement` :1352.

## control_heartbeat (relay -> node, signed, not audited)
- `RelayRuntime._publish_control_heartbeats` app.py:728-781: `{"v":1,"t","type":"control_heartbeat","event_id","session","source":"relay","drone_id","connection_epoch","roster_version","seq"}` + signature; interval min(1.0 s, watchdog_hold_ms/2000).

## control_localization (signed non-adapter frame; template for new signed frames)
- relay/control_frames.py:44-87; handler session.py:1041-1129; contracts module relay/control_localization_contracts.py.

## Refusals / docs
- `refusal_event` contracts.py:1195-1226; `acknowledgement_event` :1156-1192; session `_protocol_refusal` :2124, `_refusal` :2108, `record_refusal` :1520, `record_lifecycle` :1471. Normative prose relay/README.md:141-278 (adapter frames 141, fan-out 173, node protocol 215, heartbeat 236, command 254, node-authored frames 268), log volume :304-314.

## Capabilities and readiness
- relay/capabilities.py: `IntentName` :11-29 (arm, disarm, estop, select, takeoff, land, land_all, hold, translate, altitude, formation_next, formation_set, spacing, come_home, sweep, capture_room, survey_area, map_area); `CapabilityProfile` :32-64 (name regex :8, enabled_intent_names, supports(), state_value()); `C1_IMPLEMENTED_INTENT_NAMES` :67-85 hard ceiling; `C1_CAPABILITY_PROFILE` name "c1_basic_control" :87-90. Derived `PlanningConfig.effective_capability_profile()` planner/planner.py:145-155 ("c1_basic_control.no_altitude"). Console pins the c1 list (contract.ts:775-779).
- Adapter capability strings (join.capabilities) are a separate namespace: `"flight"` required at relay/state.py:580-583 (`adapter_capabilities_missing`, `flight_capability_missing`); camera patterns `_CAMERA_PATTERNS = {"pano_360","reconstruct_8"}` state.py:27, projected :654-664 (bare or `camera:<pattern>`).
- `FleetRegistry._readiness_reasons` state.py:576-594 order: identity_unverified, adapter_capabilities_missing, flight_capability_missing, telemetry_missing, telemetry_stale, home_pose_missing, control_authority_missing, rc_safety_operator_missing. READY iff empty else DEGRADED (:293, :379). Home pose captured from telemetry on readiness with home_pose_confirmed (:279-289); cleared when unconfirmed while `_is_grounded` (:603-612, states {armed, disarmed, landed}).
- Telemetry consumption: `relay_snapshot` relay/autonomy.py:148-242 -> `FleetSnapshot.from_relay_state` planner/models.py:481 -> `AircraftState` :239-260 (pose, home, flight_state, battery, link_quality, link_last_seen_ms, position_quality, position_last_seen_ms, heading_deg). Drones whose telemetry.state is not a FlightState are SILENTLY EXCLUDED from the snapshot (autonomy.py:94-95, :204-215). `FlightState` models.py:92-101 (disarmed, landed, armed, taking_off, airborne, hovering, landing, emergency); `AircraftState.airborne` :319-326 drives ~15 arbiter gates; `RefusalReason` :138-176.
- Arbiter arbiter/safety.py: geofence+ceiling `check_command` :672-684; altitude floor :660-666; `check_altitude_outcome` :714-791; `_check_spacing` :1975-1997 (3-D distance < min_spacing_m); `_check_vertical_motion` :1923-1973; `projected_positions` :1999; `command_position` :2010-2022; `_check_telemetry` :1779-1838; `_check_battery` :1841; airborne gates :1698-1768. `SafetyConfig` :90-109 fields: geofence, ceiling_m, min_spacing_m, battery_reserve_fraction, battery_critical_fraction, battery_cost_per_m, min_link_quality, max_link_age_ms, min_position_quality, max_position_age_ms, operator_timeout_ms, max_future_clock_skew_ms, min_capture_storage_bytes, max_capture_pose_drift_m, max_capture_gimbal_error_deg, positioning_loss_hold_ms, motion_conflict_window_ms. `Geofence` models.py:204-211 (min/max x,y,z).
- Planner planner/planner.py: `_FORMATIONS` :741 (line, column, circle, grid, V), `_formation_targets` :751-812 (planar offsets at mean z), `_sweep_lanes` :814; `PlanningConfig` :47-64 (takeoff_altitude_m, translation_step_m, flight_speed_m_s, capture_*, reconstruct_headings_deg, translation_frame world|aircraft_relative :130-131, altitude_step_m, altitude_floor_z_m, altitude_configuration_id, altitude_completion_tolerance_m, spacing_step_m). `planner/roster.py:91-129 authorize_graceful_removal` requires DISARMED/LANDED.

## Relay -> console stream
- relay/app.py: `/ws/{session_id}` `websocket_relay` :956-1076; `RelayRuntime.publish` :654-697 (control_pose only to matching adapter; stale roster_version membership/state dropped); `_fanout_loop` :702 at settings.fanout_hz (10 Hz, settings.py:112-113) -> `periodic_events` :820 -> session.py:1748; `process_frame` :853; `adapter_disconnected` :793; `cleanup_connection` :602. `_OUTBOUND_LIMIT = 128` :49.
- Event types on the console socket: auth.accepted, auth.refused, membership, state, telemetry, safety_action, acknowledgement, refusal, capabilities, capture_readiness, node_status (README:213). Node sockets also get command, control_heartbeat.
- Video state (#166): `project_video` relay/media.py:265-288 -> `{"status": live|offline|unreported, "last_frame_at"}` embedded per drone by `_aircraft_state` state.py:702-711; `MediaMonitor` media.py:133-262 polls `/v3/paths/get/{stream_name(id)}`; `stream_name(drone_id) -> f"drone{drone_id}"` media.py:30-32; `DEFAULT_MEDIA_DRONE_IDS = (1,2,3,4)` :25; wiring `default_media_monitor` app.py:59-74. `/runtime-config.json` app.py:1102-1110 (bearer) -> `RelaySettings.media_runtime_config` settings.py:243-251 (webrtcOrigin, readerUsername, readerPassword). Other HTTP: GET /metrics, GET /session/{id}?after_sequence=, POST /api/sessions/{id}/transcripts.

## Fake node and vectors
- adapters/dji_mini3/fake_node.py: `FakeNodeConfig` :48-79 (relay_url, session, drone_id, token, adapter_id, telemetry_hz, capabilities=("flight","pano_360","reconstruct_8"), home, fov, gimbal, photo sizes, storage, silent_operations, slow_operations, slow_ack_delay_s); `_Aircraft` :82-92; `FakeNode` :95 (run :119, start :172, stop :191, _send_loop :198, _receive_loop :204, _telemetry_loop :230, _handle_membership :237, _handle_command :260, _admission_refusal :293, _execute :317, builders _envelope :394, _signed_membership :403, _acknowledgement :413, _telemetry_frame :433, _capabilities_frame :451, _node_status_frame :474, _capture_readiness_frame :488, _media_record :508, _media_file_frame :547; CLI :555-618). `just fake-node`.
- adapters/dji_mini3/vectors.py: generates fixtures into `adapters/dji_mini3/pilot-app/bridge-core/src/test/resources/vectors/` (:25-33): canonical_json_cases :50, hmac_cases :98, command_args :147, command_unsigned :165, node_settings :189, frame_vectors :199-457, render :460, write :472. Refresh `uv run python -m adapters.dji_mini3.vectors`. Tests adapters/dji_mini3/test_vectors.py (`test_fixture_files_are_current`), relay/tests/test_bridge_roundtrip.py (FakeNode against in-process relay, remote backend, build_dispatcher), relay/tests/test_autonomy_roundtrip.py, adapters/dji_mini3/test_remote.py. Payload fixtures relay/tests/conftest.py (membership_payload :114, telemetry_payload :150, acknowledgement_payload :180, command_payload :221, capabilities_payload :260, node_status_payload :416, SESSION, CONSOLE_KEY, ADAPTER_KEY, MutableClock, EventIds, profiled_sink).

## Settings
- relay/settings.py `RelaySettings` :34-279, `from_env` :149-241. Env: SWEEP_RELAY_TOKEN, SWEEP_ADAPTER_KEYS_JSON (`_credential_keys` :282-300, int ids 1..2**31-1, <=64), SWEEP_LOCALIZATION_KEYS_JSON, SWEEP_ALLOW_SHARED_ADAPTER_TOKEN, SWEEP_ADAPTER_BACKEND (sim|remote :29-31), SWEEP_COMMAND_TTL_MS, SWEEP_COMMAND_DEADLINE_MS, SWEEP_VIRTUAL_STICK_HZ, SWEEP_NODE_WATCHDOG_HOLD_MS/FAILSAFE_MS, SWEEP_TELEMETRY_FRESHNESS_MS, SWEEP_TRANSPORT_EVENT_MAX_AGE_MS, SWEEP_FUTURE_CLOCK_SKEW_MS, SWEEP_INTENT_MAX_AGE_MS, SWEEP_AUDIT_STATE_INTERVAL_MS, SWEEP_STATE_MEMBERSHIP_HISTORY, SWEEP_SESSION_LOG_DIR, SWEEP_CONSOLE_ORIGINS; media SWEEP_MEDIA_API_URL/USERNAME/PASSWORD/TIMEOUT_MS/POLL_INTERVAL_MS/STALE_AFTER_MS, SWEEP_MEDIA_WEBRTC_ORIGIN, SWEEP_MEDIA_READ_USERNAME/PASSWORD. `SWEEP_SAFETY_JSON`/`SWEEP_PLANNING_JSON` read by `AutonomyConfig.from_env` relay/autonomy.py:120-145 (keys must be exactly the dataclass fields). `tests/test_env_example.py:5-46 ACTIVE_ENVIRONMENT_KEYS` must match `.env.example` exactly.
- MediaMTX: docker-compose.yml:22-36 (`MTX_AUTHINTERNALUSERS_0..3_PASS=${SWEEP_MEDIA_DRONE1..4_PASSWORD}`, _4 reader, _5 api; positional against media/mediamtx.yml:44-71 accounts drone1..4, sweep-reader (read/playback on `~^drone[1-4]$`), sweep-api; api :9997). Password derivation media/README.md: `printf %s 'sweep-media-publish-v1:drone1' | openssl dgst -sha256 -hmac "$TOKEN" -binary | xxd -p -c 256`.

## Audit sampling (#181) to copy for sensor frames
- relay/audit.py `SessionAuditLog` (begin_operation :107, append :134, append_batch :138-178, _validate_event :229, _encode_record :238 MAX 1 MiB). Session: `_audit_operation` :2216, `_append_audit` :2347, `_commit_audit_batch` :2392, undo hooks `_remember_*`.
- `_AuditSampling` :264-275; `_state_projection_changed` :2366; `_state_keepalive_due` :2369; `_sample_state_audit` :2377; `_record_state_audit` :2385. `process_node_frame` :1196-1206 appends every node frame; state appended + sampled only for Capabilities/NodeStatus.
- Bounded projectors FAIL CLOSED on unknown keys: `_material_state_projection` :2539-2576, `_MATERIAL_STATE_PASSTHROUGH_KEYS` :2427-2446, `_material_drone_projection` :2645-2703 vs `_DRONE_STATE_KEYS` :2447-2472, `_VOLATILE_DRONE_KEYS` :2473, `_TIMESTAMPED_DRONE_REPORTS` :2478-2482, `_DRONE_REPORT_FIELDS` :2483-2522. `_material_drones_projection` :2579-2580 raises when len(drones) > MAX_PHYSICAL_AIRCRAFT. Tests relay/tests/test_audit_sampling.py.

## Aircraft-only assumptions (must be class-aware)
- `MAX_PHYSICAL_AIRCRAFT = 4` state.py:24, enforced in apply_join :210-214 (fleet_capacity) and audit bound session.py:2580.
- state.py:580-583 flight gate; :27 camera patterns; :28/:523-527 formations; :603-612 _is_grounded; :670-711 `_aircraft_state` projection names (flight_state, camera_patterns, home_pose, rc_safety_operator_present, video).
- contracts.py:362-367 CapabilitiesFrame aircraft fields; :592-642 NodeStatusFrame phone fields; :129-148 command set has no ground-drive op.
- media.py:25 DEFAULT_MEDIA_DRONE_IDS; planner/models.py FlightState :92-101, airborne :319-326, RefusalReason :138-176 (AIRCRAFT_NOT_READY, CEILING, ARMED_REQUIRED, RC_SAFETY_OPERATOR_ABSENT, HOME_POSE_MISSING); planner.py:741-812 planar formations at mean z; safety.py 3-D geofence/ceiling/spacing and airborne gates :1698-1768; roster.py:91-129; autonomy.py:94-95/:204-215 silent exclusion; language/relay_compiler.py:436-529 `D-NN` labels and aircraft phrasing; intent_v1.py:67-68 MAX_INTENT_DRONE_IDS=6.
- IDs are positive ints on the wire everywhere; labels `D-{id:02d}` in relay_compiler.py:436-437 and console.
