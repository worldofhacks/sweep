# Sweep console map (main @ cf73068) for ground robots, lidar, Devices module

Repo: /Users/quietguy/capystone/console. Vite 8 + React 19 + TS 6, pnpm, vitest (jsdom). Verify with `pnpm lint && pnpm test && pnpm build` (build runs `tsc -b`; there is no typecheck script). tsconfig has noUnusedLocals/noUnusedParameters/erasableSyntaxOnly on.

## Module registry
- `src/modules/types.ts:12-19` `ModuleId` union ('control'|'live'|'gesture'|'speech'|'captures'|'worlds'|'reference').
- `src/modules/registry.ts:12-69` `MODULES: readonly ModuleDefinition[]` in nav order; `getModule(id)` :71-75.
- `ModuleDefinition` (types.ts:50-61): `{ id, label, title, note, component: ComponentType<ModuleProps>, context: ComponentType<ModuleProps> }`.
- `ModuleProps` (types.ts:37-48): `{ controller: ConsoleController; catalog; now; roomId; onRoomIdChange; services: ModuleServices; media?: MediaRuntime }`.
- Shell routing: `src/shell/Shell.tsx:45` useState<ModuleId>; :89 Rail, :90 module component, :92-95 ContextColumn, :105 TabBar. Rail `src/shell/Rail.tsx`, TabBar `src/shell/TabBar.tsx` (special-case 'reference').
- Panes: `<Pane title note tabs activeTab onTabChange tabsLabel tabsVariant>` from `src/shell/Pane.tsx:52-88`; `PaneTabs` :18-39. Eyebrows are per-module CSS classes; generic `PanelHeading`/`.eyebrow` and `EmptyModule({what, detail})` in `src/modules/shared.tsx`.
- Tokens: `src/tokens.css` (`--color-map-grid: #F8F7F4` at :15 is unused; `--color-stream-live/-offline/-unreported` :44-46). Per-module css imported by the module component (e.g. `live.css` from LiveModule.tsx:2).
- Adding a module: add id to ModuleId; add entry to MODULES; create `src/modules/<name>/<Name>Module.tsx` wrapping Pane + css; context usually `FleetContext` (`src/modules/FleetContext.tsx:13-15`); UPDATE `src/shell/shell.test.tsx:91-101` (asserts exact rail labels and length 7).

## Relay client, contract types, store
- `src/relay/contract.ts` mirrors relay/intent_v1.py: `DroneId = number` :9; `IntentSource` :11; intent names/sets :28-162; `MembershipState` :164-170; `MediaStreamStatus`/`MediaStreamState {status,last_frame_at}` :237-242; `RelayAircraftState` :244-265 (drone_id, connection_epoch, membership, readiness_reasons[], flight_state, battery, link, pos_quality, control_authority, rc_safety_operator_present, last_seen_at, camera_patterns[], selectable, adapter_id, adapter_capabilities[], home_pose: unknown, telemetry: unknown, membership_history[], membership_history_truncated, video?); `RelayStateEvent` :267-290 (roster_version, state_sequence?, armed, estop, selection, formation, spacing, mode, capability_profile, enabled_intent_names, pending, accepted_plan, drones[], ...); `RelayMembershipEvent` :292-321 (capabilities: string[]); acks/refusals :323-384; `RelayTelemetryEvent {drone, connection_epoch, x,y,z,vx,vy,vz,battery,state,link,pos_quality}` :386-405; `RelaySafetyActionEvent` :407-418; `RelayServerEvent` union :420-428; voice plan types :445-700; `isDroneId` requires positive integer :720-722; `MAX_INTENT_DRONE_IDS = 6` :21; `isCapabilityAdvertisement` :764-780 pins c1_basic_control to the 15-name list; `isRelayAircraftState` :802-830; `parseRelayServerEvent` :856-1026 fails closed.
- WebSocket client `src/relay/client.ts` (`WebSocketRelayClient` :36-167, `buildSessionWebSocketUrl` :208-220 -> <base>/ws/<sessionId>). Runtime `src/relay/runtime.ts:36-86` builds four clients (console, keyboard, webcam, language). Bootstrap `src/relay/bootstrap.ts` (`/relay-bootstrap.json`; vite.config.ts:17-34 serves it from SWEEP_RELAY_TOKEN/ORIGIN/SESSION_ID).
- Store `src/control/state.ts`: `ControlState` :91-116 (`aircraft: Record<DroneId, RelayAircraftState>` :98, selection, formation, spacing, capabilityProfile, enabledIntentNames, departed, requests, selectedFeedId, capturePattern, armed, estop, notices); `ControlAction` :118-133; `controlReducer` :206-262; `reduceRelayEvent` :359-415 (telemetry DROPPED at :368-372); `reduceStateEvent` :489-652; `reduceMembershipEvent` :654-726; `isIntentEnabled` :183-185; `capabilityBlockedReason` :187-196; `formatDroneId` :1045-1047 (`D-${pad2}`); inline `D-` at :382 (safety_action notice).
- Hook `src/control/use-control-console.ts`: subscriptions :82-141; controller surface :681-701 (state, pendingRequest, issueIntent, toggleAircraft, selectAircraft, selectAllReady, prepareCapture, prepareHold, prepareIntent, prepareSelect, prepareVoicePlanStep, confirmRequest, cancelRequest, issueHold, issueNetworkStop, changeCapturePattern, invalidatePending, retryRequest, selectFeed).
- Media runtime config `src/media/runtime-config.ts` (`/runtime-config.json`; needs webrtcOrigin/readerUsername/readerPassword); source order in `src/main.tsx:62-66`; vite dev endpoint vite.config.ts:43-67.

## Labels (D-0n) and "aircraft" copy
- formatDroneId consumers: shell/Dock.tsx:83; shell/derive.ts:100,121,124; shell/format.ts:22; catalog/derive.ts:160; gesture/use-gesture-producer.ts:577,583; modules/captures/CapturesModule.tsx:142; modules/FleetContext.tsx:104,186; modules/connectivity/ConnectivityModule.tsx:143; modules/gesture/TargetStrip.tsx:99,102,155,160,195; modules/speech/SpeechModule.tsx (many); modules/live/FocusFeed.tsx:26; modules/live/Mosaic.tsx:93; modules/control/RequestsPane.tsx:82; FleetPane.tsx:81,161; CapturePane.tsx:210; controls.ts:73,348,381,420,439,551,597,602,725; control/state.ts:718,970; control/plan.ts:19; media/LivePlayer.tsx:36.
- "aircraft" copy: shell/sentences.ts (REASONS :5, READINESS :56, MEMBERSHIP_REASON :69, INVALIDATION :80, reasonSentence :92; lines 18,21-24,27,31,39,40,43,46,50-52,60,62,66,73,85,86); controls.ts NO_SELECTION_REASON :57, NO_READY_REASON :58, :153,169,547,566,599,612,617,673,725; live/Mosaic.tsx:102,104,166,170; derive-live.ts:93,95,98; LiveModule.tsx:46; FocusFeed.tsx:28,44,63,78; TargetStrip.tsx:50,95,143,177,179; SwarmPane.tsx:61,69,103,293-299; FleetPane.tsx:25,45,56,85,87; FleetContext.tsx:33,110; CapturePane.tsx:74; ConnectivityModule.tsx:51-52; Shell.tsx:82; Header.tsx:150.
- Takeoff/land/altitude UI: controls.ts:161 'Takeoff', :166-169 'Land all', :190-191 altitude, :261 'Land', :383-387 fan-out copy, :674-681; CommandsPane.tsx:100-101; TargetStrip.tsx:17,34-52 QUICK_COMMANDS; control/plan.ts:4-10 PLAN_TITLES, :31-44, :49; gating controls.ts:107; use-control-console.ts:252,417,665; gesture/policy.ts:67-79; speech/compiler.ts:65-67,126-128,150,255.
- Recommended seam: `formatDeviceId(id, deviceClass)` + `deviceNoun(deviceClass)` exported from control/state.ts, keep `formatDroneId` as alias.

## Live module
- `LiveModule.tsx:25-66` panes 'wall4'|'wall6'|'focus' (:12-18); roster via `sortedAircraft(state.aircraft)` (:28); `<Mosaic count>` :48-63.
- `Mosaic.tsx:29-68` (props :9-21); `mosaicSlots(aircraft, count)` derive-live.ts:85-90 takes first N by drone_id; `Tile` :70-154 mounts `<LivePlayer>` only while `stream.status==='live' && media` (:97,:109). `FocusFeed.tsx:25-83` (copy "Stream names are derived as drone{id}" at :48-51).
- Video state = `RelayAircraftState.video` only (`deriveStream` derive-live.ts:31-42). Console never probes MediaMTX.
- Path mapping: `streamName(droneId) => \`drone${droneId}\`` `src/media/playback.ts:30-32`; `createPlaybackDescriptor` :34-52 THROWS unless droneId integer 1..6 (:35-37); URL `<webrtcOrigin>/<stream>/whep`. `LivePlayer.tsx:18-45`, `use-playback.ts`, `player.ts` (WhepPlaybackSession), `media/runtime.ts:13-15`.
- Tests: live.test.tsx (458 lines), derive-live.test.ts.

## Fleet map: does not exist
- Only canvas: gesture landmark overlay `src/gesture/overlay.ts:5-20` (resize + 2d context pattern). Reference > Map tab is an honest empty (`ReferenceModule.tsx:10,17,42-46,81-83`) and is the intended home per docs/design/console-brief.md:101.
- Closest 2D plot: Control > Swarm formation panel, CSS absolute positioning: `formationSlots(name,count,spacing)` controls.ts:304-322 (metres, room frame); `formationPlot` :337-359 (span = max(1.2, max|x|,|y|)*2.4; left/top percent :350-351); rendered SwarmPane.tsx:231,263-273.
- Frames: `TranslateArgs {dx,dy}` room frame east/north positive (contract.ts:174-180); `SweepBox` :187-192; telemetry x,y,z (:395-400) but discarded by the reducer; `RelayAircraftState.telemetry`/`home_pose` typed unknown (:260-261).
- A map needs: typed pose source (parse telemetry into ControlState or type `telemetry`), a scan event in RelayServerEvent + parse branch, and one <canvas> with ordered draw passes (occupancy raster, scans, poses, geofence).

## Control module gating
- Capability source: `capability_profile` + `enabled_intent_names` (contract.ts:281-282; validated :764-780); copied at state.ts:524-525. `gateControl(state,name,options)` controls.ts:95-109 (connection -> capability -> selection rule -> readiness -> extra -> isSupportedIntent -> estop); `control()` :111-140 -> `ControlSpec` :31-45 (badge '' | 'confirm' | 'unsupported'). Groups: fleetControls :143-156, motionControls :159-176, formationControls :182-186, altitudeControls :188-193, commandCatalog :247-272, laterRow :231-244. Unsupported never hidden (`ControlButton` SwarmPane.tsx:147-179; CommandsPane.tsx:36-53).
- Panes: ControlModule.tsx:36-74 (`ControlPaneId` 'swarm'|'capture'|'commands'|'requests'|'fleet').
- Confirmation: `requiresConfirmation` contract.ts:116-126 (takeoff, land, land_all, sweep, capture_room) -> `stageForConfirmation` use-control-console.ts:189-211 -> `buildPlanPreview` control/plan.ts:64-77 -> `<Dock>` shell/Dock.tsx:22-149; `confirmRequest` :491-574.

## Tests / CI
- package.json scripts: dev, build (tsc -b && vite build), lint (eslint .), preview, test (vitest run), test:m14-browser (scripts/m14-browser-smoke.mjs: spawns python sim adapter on :18765 + vite :14173 + headless chromium).
- CI `.github/workflows/ci.yml` job console (:31-51: pnpm install --frozen-lockfile, lint, test, build) and m14-browser (:91-115).
- Fixtures: `src/testing/fixture-relay-client.ts` (`FixtureScenarioName` 'control'|'pending4'|'six6'|'down' :27-38; `designDrone` :932; `fixtureAircraft(now, fleetSize)` :1046) enabled in DEV via `?fixture=` (main.tsx:19-23).
- Key tests: shell.test.tsx:91-101 (rail list), App.test.tsx, live.test.tsx, derive-live.test.ts, ControlModule.test.tsx, controls.test.ts, state.test.ts, contract.test.ts, playback.test.ts, Dock.test.tsx, derive.test.ts, ReferenceModule.test.tsx, TargetStrip.test.tsx.
